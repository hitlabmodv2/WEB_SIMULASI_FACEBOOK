dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.upm
tmp/
bin/
replit.md
.local
.cache
.cache/
.config
data
index.d.ts

# Force-include .replit
!.replit

# File tidak perlu di-push
test_preview.pdf
zipFile.zip
*.zip
*.pdf


# Sessions — single-file format (.json) di-upload, folder lama di-exclude
sessions/**
!sessions/
!sessions/*.json

# Jadibot — upload seluruh isi folder (termasuk consolidated session)
!jadibot/
!jadibot/**

.token
.token.secret
.repo.last

# node_modules — exclude semua (install ulang via npm install)
node_modules/

# PM2 logs — jangan di-push ke git
logs/*.log
!logs/.gitkeep
